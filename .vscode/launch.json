{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Unity Editor",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}"
        },
        {
            "name": "Attach to Unity",
            "type": "vstuc",
            "request": "attach"
        }
    ]
}